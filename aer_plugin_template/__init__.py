"""AER plugin template — copy this repo to scaffold a new AER plugin.

This package is intentionally minimal. The real value of this repository
is its file structure (workspace.toml, pyproject.toml, setup.sh, tests,
and CI configuration), which you should copy and adapt for your own plugin.
"""

__version__ = "1.0.0"
